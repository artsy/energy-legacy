

@interface ARNavigationBar : UINavigationBar

@property (nonatomic, assign) BOOL suppressAddingAppleNavButtons;

/// Extends the height so that there is a margin around buttons
@property (nonatomic, assign) BOOL extendedHeight;

@end
